/**
 * Federation entry for the dashboard MFE. The shell lazy-loads this default
 * export with the active workspace id. Providers come from the host tree.
 */
export { default } from "./DashboardApp.js";