 Dashboard UI Global store from task

## Overview

In shell app header add notification icon with tooltip show toDo, In progess task with count using shared state package using Zustand and also move all the store logic into @package/store

## Requirements 

- make global store that can communicate with shell and remove
- store should presist even after refresh
- add notification icon on right side to display tasks in tooltip if required create a component for tooltip.